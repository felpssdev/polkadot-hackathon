[package]
name = "polkapay_escrow"
version = "1.0.0"
authors = ["PolkaPay Team"]
edition = "2021"

[dependencies]
ink = { version = "4.3", default-features = false }

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "ink/std",
]
ink-as-dependency = []

